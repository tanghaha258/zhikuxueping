"""学情改进与二次评价领域（计划 Task 7）。

模块组成：
- ``policy``：权限策略，复用项目管理权限，确保跨教师/学校隔离不放宽。
- ``repository``：函数式持久化操作，唯一提交点在服务层。
- ``service``：建议引用证据校验、采用/修改/拒绝留痕、建议转任务/测评、二次评价关联。
- ``router``：FastAPI 路由，前缀 /api/v1/improvements。

阶段验收：教师可以从评价结果创建改进任务，并通过二次评价验证变化。
"""
