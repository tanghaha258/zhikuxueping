# 智跨学评智能体执行规则

## 权威来源

按以下优先级理解和实施需求：

1. `docs/superpowers/plans/2026-07-24-zhi-kua-xue-ping-full-rebuild.md`
2. `docs/superpowers/specs/2026-07-24-zhi-kua-xue-ping-redesign.md`
3. `00_项目总览.md`、`01_PRD_产品需求说明.md`、`02_业务流程与信息架构.md`
4. 当前代码、Alembic迁移和自动化测试所反映的现状

发生冲突时以前一层为准。工作区外的历史备份仅用于误删恢复，不是需求或设计依据。

## 开始任务前

1. 先完整阅读总实施计划的第0节、所属Task、阶段验收标准和测试边界。
2. 确认该Task的依赖波次已集成，不得跳过阶段门禁。
3. 使用独立Git worktree和独立分支；禁止在 `main` 上开发，禁止多个会话共享同一工作区。
4. 从最新集成分支开始，每次只承担一个Task，并严格遵守该Task的Files范围。
5. 先运行相关基线测试；基线失败时记录并交给集成会话决定，不得自行忽略。

## 会话内子代理

- 子代理可以并行承担互不重叠的后端、前端、测试和审查工作。
- 同一文件、同一迁移链、同一路由表或同一共享DTO只能有一个写入者。
- 主会话负责汇总子代理结果、审查差异并运行该Task的全部验证，不得直接转交未经审查的修改。

## 实施纪律

- 先写失败测试，再实现，再运行计划列出的验证命令。
- 不引入模拟业务结果、随机学情、假保存、假成功或绕过权限的演示逻辑。
- 不新建第二套项目、任务、提交、评价或学情事实；独立工具通过上下文链接接入项目。
- 不擅自修改产品规格、总实施计划、`AGENTS.md` 或其他Task的所有权范围。
- 数据迁移必须可重复、可验证、可回滚；禁止多个会话并行新增Alembic head。

## Task交付回执

每个工作会话完成后向集成会话提交：

1. Task编号和完成范围
2. 提交SHA与分支名
3. 修改文件清单
4. 数据库/API/路由影响
5. 实际运行的测试命令与结果
6. 未完成项、风险和需要集成者处理的冲突

集成会话逐个审查并合并，完成一个波次后运行联合回归；只有阶段验收通过后才能放行下一阶段。
